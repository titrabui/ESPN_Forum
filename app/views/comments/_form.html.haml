= simple_form_for ([@message, @message.comments.build]) do |f|
  = f.input :content, label: "Leave a comments", :as => :ckeditor, input_html: {:ckeditor => {:toolbar => 'mini'}}, class: "form-control"
  = f.button :submit, class: "espn-btn espn-btn-primary"